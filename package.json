{
  "name": "@volta-oss/no-show-reducer",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "Reminder and follow-up playbooks for appointment-based local businesses.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "npm run build && node --check src/*.js && node --check test/*.js",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "clean": "rm -f src/*.js src/*.js.map src/*.d.ts test/*.js test/*.js.map test/*.d.ts",
    "ci": "npm ci && npm run lint && npm test && npm run test:backend --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "docker:build": "docker build -t volta-oss/no-show-reducer:local .",
    "docker:run": "docker compose up --build"
  },
  "keywords": [
    "ai-automation",
    "digital-equity",
    "local-first",
    "oss",
    "release",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/no-show-reducer.git"
  },
  "homepage": "https://volta-npo.github.io/no-show-reducer/",
  "bugs": {
    "url": "https://github.com/volta-npo/no-show-reducer/issues"
  },
  "author": "Volta Open Source <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^24.0.0"
  }
}
